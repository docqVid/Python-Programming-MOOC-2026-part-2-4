# Write your solution here

# This is a program that asks the user for a password and then asks them to repeat it. The program loops untill they enter the same password

password = input('Password: ')

while True:
    password_2 = input('Repeat password: ')
    if password_2 == password:
        print('User account created!')
        break
    else:
        print('They do not match!')
